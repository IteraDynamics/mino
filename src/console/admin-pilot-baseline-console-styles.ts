export const ADMIN_PILOT_BASELINE_CONSOLE_CSS = String.raw`
.identity-value {
  font-family: inherit;
  font-size: 12px;
  font-weight: 620;
}

.principal-chip {
  display: inline-flex;
  align-items: center;
  max-width: 220px;
  min-height: 28px;
  border: 1px solid var(--line);
  border-radius: 999px;
  background: var(--panel);
  color: var(--ink-soft);
  padding: 4px 9px;
  font-size: 11px;
  font-weight: 650;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.pilot-identity-grid {
  margin-bottom: 4px;
}
`;
